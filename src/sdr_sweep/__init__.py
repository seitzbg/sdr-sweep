"""sdr-sweep — wideband spectrum sweep toolkit for the USRP B210.

Capture engine, live TUI, offline analysis, and a Prometheus exporter around
one shared data format (rtl_power-style CSV + a JSONL stream).
"""

__version__ = "1.0.0"
